name: 01-building-block

on:
  workflow_dispatch:
  push:

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "hello world"

  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Failed stop
        run: |
          echo "a will fail"
          exit 1
      - name: Say goodbye
        run: echo "goodbye"